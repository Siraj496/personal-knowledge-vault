<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Page Title</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>

<%- include("partials/header") %>

<h1><%= note ? "Edit Note" : "Add Note" %></h1>

<form class="main-form" action="<%= note ? '/note/edit/' + note.id : '/note' %>" method="POST">
  <div>
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="<%= note ? note.title : '' %>" required>
  </div>

  <div>
    <label for="content">Content:</label>
    <textarea id="content" name="content" required><%= note ? note.content : '' %></textarea>
  </div>

  <div>
    <label for="tags">Tags (comma separated):</label>
    <input type="text" id="tags" name="tags" value="<%= note ? note.tags.join(', ') : '' %>">
  </div>

  <button type="submit">Save</button>
</form>

<%- include("partials/footer") %>


</body>
</html>